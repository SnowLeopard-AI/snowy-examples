[project]
name = "snowleopard-fastmcp-example"
version = "0.1.0"
description = "A simple FastMCP server hooked up to Snow Leopard via tool"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.13.3",
    "snowleopard>=0.2.0",
]
